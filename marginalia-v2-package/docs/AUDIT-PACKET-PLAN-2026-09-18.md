# Marginalia — Implementation Packet Plan (synthesis of 8 ticket audits + vision review)

**Verified in this pass** (read directly, not inferred): `daemon/jobs/host-instructions.ts:13` returns early for every intent but `define` and hard-codes `skills/define`; `daemon/jobs/envelope.ts:56` hard-codes the label `Pinned definition instructions`; `daemon/jobs/service.ts:560` already passes `input.intent`; `capabilities: ['samples']` is hard-coded at `ui/margin.ts:663` and `ui/asking-host.ts:224`; `daemon/server.ts:154` calls `store.exportThread` and no `/api/diagnostics` route exists; `skills/` holds only `define, evidence, explore`; `vocabulary` is created at `daemon/store.ts:73`, read/deleted only in `daemon/library.ts`, never written; no `README.md` or `LICENSE` in `marginalia-v2-package`; no `T09.md`, `T10.md`, `T12.md`, `T16.md`, `T17.md` receipts.

**Reserved** (owned elsewhere, never scheduled here): `ui/asking/**`, `ui/asking-host.ts`, the `ui/margin*.ts` submission path, `daemon/jobs/service.ts`, `daemon/consent/**`, `daemon/solver/**`, `contracts/solver.ts`.

**File-ownership rule applied**: hot files (`daemon/server.ts`, `daemon/library.ts`, `ui/library/index.ts`, `ui/helper.ts`, `ui/margin.ts`, `renderer/index.ts`, `daemon/store.ts`, `contracts/library.ts`, `extension/entrypoints/*`) each have exactly one owning packet per wave; where two audits proposed the same edit I merged, where they proposed different edits to one file I serialized.

---

## 1. Packets (dependency-ordered)

Format: **id** — title · tickets · files · change · contract · proving test · est_lines · depends_on · reserved overlap.

**S1 — Simulate skill bundle** · T09 · `skills/simulate/SKILL.md`, `skills/simulate/IO.md`, `tests/simulate-skill.test.ts` · Add the missing folder mirroring `skills/define/**` and `skills/evidence/**`: authoring one simulate reply beside an unchanged source, packet values untrusted, no tools/fetch/code, no execution or provenance claims; IO.md names the packet fields and the `marginalia.reply.v1` block subset. · Every parameter carries finite min/max/default/unit; a `headline:true` classification must cite an installed criterion (today only `growth-v1`, `contracts/reply.ts:620`); a stand-in model requires the illustration statement; `samples` only under the granted capability. · Both files strict UTF-8, ≤16 KiB (the loader bound), and the text still names each invariant. · 240 · — · none.

**S2 — Generalize the host-instruction loader (merge of T09-P2 and T14-P6)** · T09, T14, T15 · `daemon/jobs/host-instructions.ts`, `contracts/jobs.ts`, `daemon/jobs/envelope.ts`, `tests/jobs-instructions.test.ts` · Replace the define-only early return and the hard-coded `files` array with a frozen intent→{root, files} table (`define`→SKILL.md + references/runtime-contract.md; `simulate`→SKILL.md + IO.md; `evidence`, `explore`→SKILL.md + IO.md); widen `HostInstructionBundle.kind`; derive the outgoing part label from `bundle.kind` instead of the literal at `envelope.ts:56`. `service.ts` needs no edit. · Define's bytes and digest stay byte-identical; `hostInstructionText` still throws on kind/intent mismatch or altered bytes; no page input selects a path; every other intent still gets no bundle. · Extended `tests/jobs-instructions.test.ts`: per-intent bundles include their files verbatim with matching sha256; cross-intent calls throw `/binding/`; define digest unchanged; the prepared envelope lists a correctly labelled part. · 120 · S1 · **Yes** — adding instruction bytes and relabelling the part changes `preparedPayloadDigest`, which `ui/asking/binding.ts` and `daemon/consent/service.ts` compare. Land only with the reader-journey owner's digest fixtures updated in the same wave.

**S3 — `capabilitiesForIntent` in the contract** · T14, T15 · `contracts/reply.ts`, `tests/reply.test.ts` · Add `capabilitiesForIntent(intent)` beside the existing intent/capability sets: `evidence`→`['samples','network.citations']`, `explore`→`['samples','network.shelf']`, otherwise `['samples']`. Contract only; no call site changes here. · No existing intent's list changes; the helper is the single source both the job request and the render options will read. · `validateReply(evidenceReply, {capabilities: capabilitiesForIntent('evidence')}).ok === true`, and the same reply under `capabilitiesForIntent('define')` fails with `capability network.citations is unavailable`; same pair for `shelf`/`explore`. · 60 · — · none.

**S4 — Adopt `capabilitiesForIntent` at the three call sites** · T14, T15 · `ui/margin.ts:663`, `ui/asking-host.ts:224`, `ui/asking/**` · Replace both `capabilities: ['samples']` literals and the ask-request builder with the S3 helper. · `StartJobInput.capabilities` and the render options stay equal (the `ui/asking/binding.ts:131-146` equality check). · Existing asking-flow tests plus one case asserting an evidence job's packet and render capabilities match. · 40 · S3 · **Fully reserved.** This is the single release blocker for T14/T15 — today a citations or shelf reply fails `contracts/reply.ts:490` and `commitReply` throws. Hand to the reader-journey worker as a one-line-per-site change.

**S5 — Second-passage simulate fixture** · T09 · `fixtures/second-passage-reply.ts`, `tests/simulate-second-passage.test.ts` · One authored reply for a different passage and model, with its own source text, `headline:false` because `growth-v1` is the only installed criterion. · Validates against its own `sourceText`; every binding selector an exact substring; `classificationViews` reports `withheld`; a comment states it is authored demonstration data, not a live run. · `validateReply` ok; `computeIndependentChecks` returns `unsupported`; `calculateReply` gives finite values. · 180 · — · none.

**S6 — Prove a slider sends nothing** · T09, T18 · `tests/simulate-slider-local.test.ts` · Mount the growth fixture through `mountReply` under `tests/t05-dom.ts` and drive the number and range inputs. Test-only. · A parameter change recomputes locally, calls neither `onFollowup` nor `onRecompute`, announces `Updated locally. No model request was sent.`; out-of-range withholds the headline and fires no callback. · The packet is the test. · 150 · — · touches `renderer/**` read-only.

**S7 — Simulate assessment transform** · T09 · `daemon/transforms/simulate/assess.ts`, `index.ts`, `tests/simulate-transform.test.ts` · Pure host-side dispositions mirroring `daemon/transforms/evidence/reconcile.ts`: `headline-unchecked`, `capability-missing`, `illustration-missing`. · Grants no authority, performs no IO; classification authority stays in `contracts/host-checks.ts`. · Growth fixture clean under `['growth-v1']`, dispositions appear under each deliberate mutation. · 200 · S1 · none.

**S8 — Host grid sweep** · T09, T03 · `daemon/transforms/simulate/grid.ts`, `tests/simulate-grid.test.ts` · Re-evaluate an authored samples grid through `kernel/model.ts` and emit a `host-execution` record only on exact agreement; pure function taking an execution-id supplier. · No model turn, no solver, no network; the host never authors values; disagreement, non-finite output, >500 points, or a refused model yields no record; an imported record is never upgraded. · Reproducible 2-axis grid yields a record admitted by `validateSamplesInterpolationReadiness`; a perturbed value yields none. · 280 · S7 · wiring it to a route is a later, jobs-owned change — not in this packet.

**S9 — Commit-time evidence and explore assessment** · T14, T15 · `daemon/jobs/store.ts`, `contracts/reader.ts`, `daemon/store.ts`, `tests/evidence-commit.test.ts` · In `succeed`, for intent `evidence` call `reconcileEvidence` and for `explore` call `assessShelf` using the attempt's own egress record and frozen source version; persist `evidence?`/`explore?` on `ReplyVersion` as JSON columns beside `validation`. · An assessment is computed from that attempt's records or is absent; never derived from reply JSON; other intents unchanged. · Commit an evidence reply claiming a fetch against an empty/incomplete egress record; stored verdict `insufficient`, headline `null`, attribution `unsupported-fetch-claim`. · 220 · S4 · `daemon/jobs/store.ts` is adjacent to reserved `service.ts`; confirm the seam with the jobs owner before writing. Needs a schema migration check.

**S10 — Honest retrieval log for open-session attempts** · T14 · `daemon/jobs/service.ts`, `tests/consent.test.ts` · Call the currently-uncalled `consent.recordFetchedResources(attemptId, [], false)` when an open-session attempt settles. · `egress(jobId)[0].fetched === []` and `retrievalComplete === false`; cloud-inference jobs untouched. · Evidence attempt produces the incomplete row; a define attempt produces no event. · 70 · — · **Fully reserved** (`daemon/jobs/service.ts`). Hand over.

**S11 — Renderer: retrieval facts and routed shelf opens** · T14, T15 · `renderer/index.ts`, `tests/evidence-render.test.ts`, `tests/explore-render.test.ts` · Add optional `evidence`/`explore` assessments to the render options; per-citation meta line stating what the host observed versus what the author asserted; replace the bare shelf anchor at `:429` with a control calling `onOpenShelfItem(itemId)` and label items `suggested link, not fetched`. Absent assessment renders exactly as today. · No string says `supported` or `verified`; rendering performs no IO; opening is always an explicit reader action through `prepareOpen`. · Two citations (one observed, one unsupported) render distinctly; activating an item invokes the callback once. · 300 · S9 · the margin-side mount options land in M1 below.

**M1 — Margin wiring pass (single owner of `ui/margin.ts` this wave)** · T14, T15, T17, T19 · `ui/margin.ts`, `ui/margin-model.ts`, `ui/margin.css`, `ui/install-status.ts`, `tests/margin-header.test.ts`, `tests/margin-install-status.test.ts` · Four small additions in one window: pass `saved.version.evidence/explore` into the reply mount the way `hostReport` is passed; render the real page-identity line from S12's `meta`; add the derived `resumeMarker` ("you were here") as a third header node; render two plain saving/asking status lines in `renderSettings` from a new pure `ui/install-status.ts`. · Nothing here persists, fetches or sends; the resume marker is derived, never stored, and holds only the margin's own reading position; the asking line never claims asking is on because pairing succeeded. · Header renders with `fetch`/`XMLHttpRequest` stubbed to throw and zero writes; `statusLines` table-driven over every token/reachability/reason combination. · 320 · S11, S12 · Touches the file the submission path lives in — take one exclusive window, coordinate with the reader-journey owner, do not edit submission code.

**S12 — Local page-meta extraction** · T17 · `extension/lib/page-meta.ts`, `extension/lib/capture.ts`, `extension/lib/protocol.ts`, `extension/entrypoints/panel/main.ts`, `tests/page-meta.test.ts`, `tests/extension-protocol.test.ts` · `readPageMeta(doc)` reading `citation_*`, Open Graph, JSON-LD and `time[datetime]`; returned as a new top-level `meta` field on `Snapshot`, deliberately **not** inside `capture`. · Display-only: `SourceCapture` and the immutable `source_versions` identity index are untouched, so no migration; synchronous, DOM-only, no network, no storage write; every field bounded and truncated, absent fields omitted, never inferred. · Citation/OG/JSON-LD fixtures; oversize author truncated; malformed JSON-LD ignored; `validSnapshot` accepts a good `meta` and rejects a malformed one. · 190 · — · `panel/main.ts` is T04's; single owner this wave.

**L1 — Library test debt and export-id validation** · T11 · `tests/library-settings.test.ts`, `tests/library-routes.test.ts`, `daemon/server.ts:154` · Direct tests for every public method of `daemon/library.ts` (revision/`ConflictError`/`compatibilityKey`/`continuationIdentity`/vocabulary), plus route tests for the four T11 routes; one-line change routing `/api/export` through `library.exportThread()` so `requireId()` applies. · Every T11 route has an authenticated and a rejected assertion; the revision/compatibility pair that `daemon/jobs/service.ts:339` relies on provably changes when a model choice changes. · The packets are the tests. · 360 · — · owns `daemon/server.ts` this wave.

**L2 — Whole-library export (merge of T11-P5, T19-P5, T12 dependency)** · T11, T19, T12 · `daemon/library.ts`, `daemon/server.ts`, `ui/helper.ts`, `ui/library/index.ts`, `tests/library-global-export.test.ts` · `exportLibrary({includeRemoved})` mapping `listThreads()` through the existing `exportThread` in one transaction, returning `{schema:'marginalia.library.v1', exportedAt, threads}`; `GET /api/export-all`; an "Export everything" control that shows counts first and downloads only on a second explicit click. · One entry per listed thread, each byte-identical to that thread's single-thread JSON; no pairing token or consent secret; no download without a second deliberate action; 401 unpaired. · Three threads (one removed) under both flags; each entry deep-equals `exportThread(id)`; the store records no new event. · 260 · L1 · none.

**L3 — Export formats: Markdown then BibTeX** · T11 · `contracts/library.ts`, `daemon/library.ts`, `daemon/server.ts`, `ui/helper.ts`, `ui/library/index.ts`, `tests/library-export-formats.test.ts` · Move `ThreadExport` into `contracts/library.ts`; add pure `renderThreadMarkdown` and `renderThreadBibtex` composed with `exportThread`; two routes and a format toggle on the existing paged preview. · Output derived only from the stored payload, containing no text absent from it; no BibTeX field invented — absent title/date are omitted, not guessed; braces/backslashes/percents escaped; the existing load fence discards a superseded format response. · Markdown contains the current note text and original anchor quote and nothing outside the payload; BibTeX entry parses balanced with fields absent. · 400 · L2 · **Below the plan's own S line** — `BUILD-PLAN-24H.md:107` scopes T11 to "export JSON". Schedule only if Yash raises it (see §3).

**V1 — Canonical vocabulary contract and write path** · T16, T11 · `contracts/library.ts`, `daemon/library.ts`, `daemon/store.ts`, `tests/vocabulary-store.test.ts`, `tests/vocabulary-notes.test.ts` · Replace the bare `string` origin/status with `'looked-up' | 'used' | 'stated-familiar'` and `'active' | 'familiar'`; add `recordVocabulary`, `markFamiliar` and a deterministic `noteVocabularyTerms(noteText, sourceText)` (only phrases the reader quoted verbatim, ≤5 words, ≤8 per note) called inside the existing note-write transaction. · First record sets `firstSeen === lastSeen`; re-recording advances `lastSeen` only and never downgrades `stated-familiar`; an invalid origin throws before any write; a rolled-back note mutation leaves vocabulary unchanged; `daemon/jobs/packet.ts:38` ("No vocabulary or library matches were included…") must stay true — do not edit it. · Both test files. · 240 · L3 (file lock on `contracts/library.ts`, `daemon/library.ts`) · extraction rule is human-gated, see §4.

**V2 — Vocabulary surface** · T16, T11 · `daemon/server.ts`, `ui/helper.ts`, `ui/library/index.ts`, `tests/library.test.ts` · `POST /api/vocabulary/familiar` under the existing pairing guard; `markVocabularyFamiliar` on the client; collapse `originLabel`/`statusLabel` to the canonical three and two (drop the invented "Remembered"); a Mark-familiar button beside Delete; replace the empty-state sentence that promises a future feature. · Requires current pairing (401 otherwise); disabled when the adapter is absent; a failed mark leaves the list unchanged and restores a usable action; a late response is fenced by the existing generation counter. · Extended `tests/library.test.ts`. · 180 · V1 · none.

**D1 — Diagnostics summary and route (merge of T11-P6 and T19-P2)** · T11, T19 · `daemon/diagnostics-summary.ts`, `daemon/server.ts`, `daemon/main.ts`, `tests/diagnostics-summary.test.ts` · Pure `buildDiagnosticsSummary` with a fixed key set (appVersion, osFamily, nodeMajor, port, paired count, codexVersion, codexLogin, askingAvailable, askingReasonCode, migrations, lastStartErrorCode) and a closed reason-code map; `GET /api/diagnostics` restricted to `principal.surface === 'localhost-settings'` exactly as the consent settings routes are; `?refresh=true`; a thrown probe degrades to the existing unavailable shape. · Exactly the allowlisted keys; a note body, URL, token, home path or raw stdout fed in appears nowhere in the serialization; an unrecognised reason becomes `other`; 401 unpaired, 403 from an extension origin. · The test file. · 280 · L2 · **Do not** change the `/api/jobs` free-form `unavailableReason` in this packet — `ui/asking/flow.ts` reads it (reserved). Split that into a later coordinated edit.

**D2 — Diagnostics surfaces** · T11, T19 · `ui/helper.ts`, `ui/library/index.ts`, `ui/helper-management.ts`, `ui/helper-management.css`, `tests/library-diagnostics.test.ts`, `tests/helper-management-diagnostics.test.ts` · A Diagnostics section in library settings rendering the honest `unverified`/`unknown` fields verbatim with a Check-again button and its own load fence, plus a "Copy diagnostics" control on the helper page that formats the allowlisted fields and reports what the text does and does not contain. · The page never upgrades `unverified` into a readiness claim; no request until the reader presses; clipboard failure never claims success and leaves the text selectable. · Both test files. · 300 · D1, V2 · none.

**R1 — Revocation proven from the settings surface** · T11, T13 · `tests/library-revocation-e2e.test.ts` · Pair a localhost-settings and an extension principal, approve a grant, revoke through `POST /api/consent/settings`, assert the next `POST /api/jobs` is refused and that no dispatch reached the injected runtime; second case revokes mid-flight and asserts `consent-revoked`. Test-only. · The refusal is observed as an absent dispatch, not merely a thrown error. · The packet is the test. · 160 · — · reads reserved services; writes nothing.

**R2 — Calm startup and read-only recovery** · T19 · `daemon/main.ts`, `daemon/store.ts`, `daemon/server.ts`, `tests/helper-recovery-startup.test.ts`, `tests/recovery-mode.test.ts` · Catch `ReaderMigration` and `UnsupportedReaderSchema` beside the existing EADDRINUSE branch and print the fixed S11 copy with a `~`-relative backup path; then an opt-in read-only mode serving only `/health`, the web root and the two export routes. · No stack, no absolute home path, no pairing code, no readiness line; in recovery mode no migration runs and no write reaches the database; export still succeeds; never entered on a healthy database. · A `user_version=9` database exits 1 with the exact copy; in recovery mode `POST /api/change` returns the fixed 503 and file mtime is unchanged. · 350 · L2, D1 · owns `daemon/server.ts` this wave.

**R3 — Extension admission by registration** · T19, T06, T04 · `daemon/extension-registry.ts`, `daemon/server.ts`, `daemon/helper-management.ts`, `tests/extension-registry.test.ts` · Narrow `allowedOrigin` (`daemon/server.ts:39`) from "any syntactically valid extension origin" to the helper origin plus registered origins persisted in the existing settings table; register/remove only through the helper page's `trustedPage` guard. · An unregistered origin gets 403 before any pairing attempt and a destroyed upgrade socket; registration is impossible from an extension request or a bearer token; removing a registration does not revoke pairings. · The test file. · 230 · R2 · defence in depth, not a demonstrated break — see §5.

**R4 — Fresh-machine, restart and sign-out acceptance** · T19, T12 · `tests/fresh-machine.test.ts` · Spawn `daemon/main.ts` against an empty temp data directory with no Codex env, parse origin and code, register and pair, write a note, SIGTERM, restart, assert the note and pairing survive while asking stays unavailable. · The code is refused on second use; `/api/jobs` reports `available:false` throughout and no job is created. · The packet is the test. · 210 · R2, R3 · parses `daemon/main.ts` stdout, which T06 may change.

**I1 — Service-unit generators** · T19 · `scripts/service-units.ts`, `tests/service-units.test.ts` · Pure `windowsStartupCommand`, `linuxSystemdUserUnit`, `macosLaunchAgentPlist` returning text plus a per-user target path. · Paths with spaces and non-ASCII escaped per format; per-user locations only (never `/etc`, `/Library`, HKLM); `MARGINALIA_DATA_DIR` and `MARGINALIA_PORT` set; byte-identical for identical input. · Snapshot per platform plus determinism. · 180 · — · none.

**I2 — Install/upgrade/uninstall CLIs** · T19 · `scripts/install-helper.mjs`, `scripts/uninstall-helper.mjs`, `package.json`, `tests/install-helper.test.ts` · Two Node CLIs over I1's generators, registered as `helper:install`/`helper:uninstall`; install is idempotent and reports "updated" on a rewrite. · Uninstall never touches the data directory or a browser profile by default; `--remove-data` without `--exported` exits non-zero and removes nothing; every write stays inside the generated per-user path. · The test file with HOME/LOCALAPPDATA redirected. · 280 · I1 · owns the `package.json` scripts block with T12-P3.

**X1 — WebMCP capability detection and read tools** · T10 · `extension/lib/webmcp.ts`, `extension/entrypoints/content.ts`, `extension/entrypoints/background.ts`, `tests/webmcp.test.ts` · Injectable `findModelContext(scope)`, a refusal envelope and a `tool()` wrapper; register `get_reading_state`, `get_section_text`, `get_context`, `search_notes` answering only from the existing snapshot, each gated behind a per-page reader grant stored in `browser.storage.session` under the existing `frame:<tabId>` binding. · No page agent reads one character before an explicit reader action; a page cannot self-grant; registration is idempotent; a hostile `registerTool` that throws downgrades to `unsupported` and never breaks capture; excluded sites refuse first. · Null/throwing scopes, double registration, ungranted refusals with non-empty `next_step`, refusal again after `clear()`. · 440 · S12 (extension file lock) · **Open question first**: whether `navigator.modelContext` is visible to an isolated-world content script. If a MAIN-world script is required, this packet changes shape and needs the extension trust-boundary owner.

**X2 — `insert_reply` as a proposal** · T10 · `extension/lib/webmcp.ts`, `extension/entrypoints/content.ts`, `extension/entrypoints/background.ts`, `extension/entrypoints/panel/main.ts`, `tests/webmcp.test.ts` · The handler calls the identical `parseAndValidateReply(json, {sourceText: snapshot.capture.text})` with no capability list, then posts one pending proposal per frame binding; the panel picks it up on its existing poll. · Nothing renders or persists from the tool alone; `{ok:true, state:'awaiting_reader'}`; oversize payloads refused before parsing; unresolvable source bindings refused; one pending record per page. · Growth fixture accepted, mutated source refused, a `samples` block refused, two rapid inserts leave one record. · 280 · X1 · none.

**X3 — Reader acceptance with distinct provenance** · T10 · `ui/margin.ts`, `ui/persistence.ts`, `tests/webmcp-margin.test.ts` · `pendingProposal` + `onAcceptProposal` on MarginOptions; an inert notice in the saved-replies section; Accept synthesizes a version triple through the existing `replies.cache(origin, …)` with origin `webmcp:<pageIdentity>` so the existing validate-and-mount path is reused unchanged. · An accepted proposal is labelled as the assistant-on-page path and can never be presented as a daemon reply, a saved-sample rerun or a zero-model solver rerun; helper reconcile must ignore `webmcp:` origins; declining discards. · Nothing renders before Accept; a helper refresh cannot mutate the record; the export blob keeps the origin. · 290 · X2, M1 (serialized `ui/margin.ts`) · same margin window discipline as M1.

**T1 — Release gate ledger as data** · T12 · `contracts/release.ts`, `docs/RELEASE-GATES.md`, `tests/release-gates.test.ts` · Typed gate ids and SPEC capability rows plus a pure `parseGateLedger`; the ledger starts every gate `not_started`. · A ledger missing a gate, carrying an unknown status, marking `recorded` without evidence, citing a path outside `docs/evidence/`, or lacking a 40-hex commit is rejected. · The test file. · 210 · — · the eight gate names need Yash's confirmation (§4).

**T2 — Evidence index and trackable logs** · T12 · `docs/evidence/INDEX.md`, `.gitignore`, `tests/evidence-index.test.ts` · Index every tracked evidence file with one factual line; narrow `.gitignore:7` from blanket `*.log` with `!docs/evidence/**/*.log`. · Tracked-set and index reconcile in both directions; the index is data about what was recorded, never a pass claim. · `git ls-files` vs the index, plus `git check-ignore` on a fixture log. · 180 · — · single-line `.gitignore` coordination.

**T3 — Release scripts and manifest guard** · T12 · `package.json`, `tests/release-scripts.test.ts`, `tests/extension-manifest.test.ts` · `extension:zip`, `release:build`, `release:verify`; a test comparing the wxt manifest against `package.json` and `contracts/release.ts`. · `release:verify` transitively references every check; manifest version equals package version; permissions match the documented set; CSP carries no `unsafe-eval` and no remote script origin. · Both test files. · 160 · T1, I2 (`package.json` lock) · none.

**T4 — Platform capability table** · T12, T19 · `contracts/release.ts`, `daemon/diagnostics.ts`, `tests/release-platforms.test.ts` · One row per platform with status `verified|untested|unsupported`; diagnostics reports the running platform's row instead of inventing a second notion of support; everything defaults to `untested`. · A row may read `verified` only when its evidence id resolves to a `recorded` gate; diagnostics never claims support it cannot cite. · The test file, injecting all three platforms. · 200 · T1, D1 · none.

### Human-gate packets (not scheduled)

**H1** Consent label vs runtime truth (T14-P7) — `daemon/consent/service.ts:459` promises "Codex and separate web access for this site" while `daemon/codex-policy.ts:175-181` pins `networkAccess:false` and `web_search:'disabled'`. Also touches `skills/evidence/SKILL.md`. Reserved **and** a product-honesty decision.
**H2** Broker-vs-session retrieval (T14 uncertainty) — decides the shape of S10 and H1.
**H3** WebMCP write tools and `set_section_depth` retirement (T10-P5).
**H4** `insert_artifact`/`insert_reply` reconciliation and the T10 receipt (T10-P6).
**H5** Vocabulary extraction rule (V1's `noteVocabularyTerms`) and the "looked-up" origin's home.
**H6** Assumed-terms grant semantics and Semantic Scholar (T17-P3, T17-P4) — one grant or two.
**H7** Package README/LICENSE and the root-repo supersession banners (T12-P6, T12-P7).
**H8** `docs/INSTALL.md` and truthful platform claims (T19-P10).
**H9** Recorded live simulate run on an unseen passage (T09-P7) and the T12 launch run (T12-P8).
**H10** Doc vocabulary pass over `FEATURE-INVENTORY.md` (T16-P5 plus §4B below).

---

## 2. Wave schedule

Two Sol-medium writers per wave, disjoint files, plus read-only work alongside.

| Wave | Writer A | Writer B | Read-only alongside | Waiting on reserved |
|---|---|---|---|---|
| 1 | S1 → S2 | S3 + S5 | S6, R1 (test-only) | S2 cannot merge until the reader-journey owner updates the `preparedPayloadDigest` fixtures |
| 2 | S7 → S8 | L1 | S12 (extension, disjoint) | S4 blocked on reserved owner — **the T14/T15 release blocker** |
| 3 | L2 | I1 → I2 | T1, T2 | — |
| 4 | D1 | V1 | T3 (after I2 lands `package.json`) | `/api/jobs` reason-code change waits on `ui/asking/flow.ts` owner |
| 5 | S9 | V2 | T4 | S9 needs the jobs owner to confirm the commit seam; S10 stays reserved |
| 6 | S11 | D2 | R4 drafting | — |
| 7 | **M1** (exclusive `ui/margin.ts` window) | R2 | — | M1 must not touch the submission path |
| 8 | R3 → R4 | X1 | — | X1 blocked on the isolated-world question |
| 9 | X2 | L3 *(only if §3 raises T11 scope)* | — | — |
| 10 | **X3** (second exclusive `ui/margin.ts` window) | — | — | — |

Serialized-file queues: `ui/margin.ts` = M1 → X3 (never concurrent). `daemon/server.ts` = L1 → L2 → D1 → R2 → R3. `ui/library/index.ts`/`ui/helper.ts` = L2 → L3 → V2 → D2. `contracts/library.ts`/`daemon/library.ts` = L3 → V1. `package.json` = I2 → T3. `extension/entrypoints/*` = S12 → X1 → X2. `renderer/index.ts` = S11 only.

---

## 3. Ticket modifications the audits justify

1. **T09 scope — the authoring half is missing, not partial.** Current: `BUILD-PLAN-24H.md:102` "skill folder with instructions, schema, checks". Proposed: "skill folder `skills/simulate/**` loaded by the generalized per-intent host-instruction table and visible in the exact-outgoing preview; checks are host-side (`contracts/host-checks.ts`) and the skill only declares them." Reason: no `skills/simulate` exists; the loader is define-only.
2. **T09 done-means — "a second, genuinely unseen passage yields a correct model".** Proposed addition: "with a withheld headline where no installed criterion backs it; an authored fixture satisfies the deterministic half only, and a recorded live run satisfies the criterion." Reason: `growth-v1` is the only installed criterion, so a verified headline on a second passage would require new host analysis.
3. **T09/T18 — "Moving a slider costs no inference".** Current T18 receipt: "No inference/network trace has been captured; deferred." Proposed: keep the deferral wording and add "a node:test asserting zero callbacks and the local-recompute announcement is the mechanical half" (packet S6). Reason: `docs/FABLE-TICKET-STATUS.md` lists this under Exists; the receipt is the honest one.
4. **T10 blockers.** Current: `BUILD-PLAN-24H.md:106` lists T05 as the only dependency. Proposed: "depends on T03 (validation), T04 (content-script registration) and T05 (margin acceptance)." Reason: `docs/SCOPE-COVERAGE.md:223` and every packet above touches `extension/**` and `contracts/reply.ts`.
5. **T11 export scope.** Current: `BUILD-PLAN-24H.md:107` "export JSON". Proposed either "export JSON; whole-library export" (adding L2, leaving L3 as B-tier) or "export JSON/Markdown/BibTeX; whole-library export". Reason: the area brief and `FEATURE-INVENTORY.md:49` disagree with the plan row. **Yash picks.**
6. **T11 done-means — "a revoked grant stops sends".** Proposed: "…proven through the localhost settings surface, observed as an absent dispatch." Reason: today it is proven only inside the consent and job services.
7. **T14/T15 acceptance.** Current: `:104-105` "the reply separates the claim from dated support", "end of the post yields a shelf". Proposed: add "capabilities are derived per intent (`capabilitiesForIntent`), so a citations or shelf reply can be committed at all." Reason: hard-coded `['samples']` makes both journeys unreachable.
8. **T16 vocabulary.** Current: `docs/FABLE-TICKET-STATUS.md:71` "vocabulary origin/delete services". Proposed: "vocabulary read/delete services exist; no write path exists, so the section is structurally empty until V1 lands." Also `SPEC-FINAL.md:80`'s canonical `looked-up / used / stated familiar` becomes the typed union; the invented status word "Remembered" is dropped.
9. **T19 receipt.** Current: "C1 to C15 … None is started." Proposed: mark C1, C2, C3, C4, C5, C6, C8 (store half), C11 and C13 as implemented with their line references, and keep C7, C9, C10, C12, C14, C15 outstanding. Reason: nine items verifiably landed after the receipt was written.
10. **T12.** Current: `docs/FABLE-TICKET-STATUS.md:55` "package/build tooling" under Exists. Proposed: "build tooling exists (`npm run build`, `npm run extension:build`); no release tooling, no README or LICENSE in the distributable package, no gate ledger."

---

## 4. Vision deltas

**Become packets (schedule as above or add):**
- *Egress record the reader sees* (SPEC DoD 6) → the reader-facing surface is unticketed; **add a packet** to the T13/T11 settings queue after D2, reusing D1's allowlist discipline. Highest-fidelity risk of the three named in the Definition of Done.
- *Accessibility* (SPEC DoD 7: keyboard-only, screen reader hears "ready" without losing focus, both themes, 200% zoom, reduced motion, ≥24 px targets) → **new packet, no owner today, blocking stage 4.** Recommend a dedicated axe/keyboard-harness packet sized ~300 lines, scheduled in wave 6 alongside D2.
- *Suggestion score v1 + exposure log* (whitepaper §7: "Suggestions come from what the block is, what the page is") → **new packet**, gated at stage 3; T05 ships a static table and neither the additive score nor the exposure log.
- *Narrow-width sheet below 900 px* → **T05 done-means addition**, small.
- *Packet honesty — deterministic, flagged truncation and a list of what was left out* → belongs in the reader-journey (reserved) first-send sheet; **flag to that owner**, do not schedule here.

**Become ticket modifications:**
- *Automatic definitions* ("a fast contextual definition arrives without a click", under site grant + reader setting) → add the setting, the grant precondition and a dismiss test to T08's done-means.
- *Bottom zone of the margin* ("threads on this page, related in your library, save, park, hear it") → T05 zone modification; `think with it` / `go further` are already implemented with no ticket and should be written into T05's acceptance.
- *Stated context field* — implemented in `ui/asking/mount.ts` with no governing acceptance → SPEC-FINAL amendment plus a T08 acceptance line. Built-without-governance is its own fidelity problem.
- *Thread forking on provider or permission change, and Codex isolation (no inherited MCP servers, skills or environment)* → T02 done-means addition; both are provenance- and security-relevant.
- *Derive and instantiate as launch intents* — the renderer will carry `steps` and `compare` with no skill authoring them → either T09 gains `skills/derive/**` and `skills/instantiate/**` (the S2 table already generalizes to them at near-zero extra cost) or an explicitly named unsupported state.

**Need Yash's explicit deferral decision — never dropped:**
- *Media blocks*: schema carries `media`; T18 ships stubs. Confirm stubs + an honest unsupported state satisfy "capability-gated by provider, declared now", and add `media` to the release table either way.
- *Instant tier* (page type, structure, difficulty, no model): inventory says S, SPEC-FINAL is silent — resolve the contradiction by amendment or deferral.
- *Hear it* (voice) and *related in your library* (CiteSee-style): in SPEC's launch margin description, in no ticket. Defer with an honest state rather than a missing affordance.
- *Sharing a thread with excluded, previewed source excerpts*: inventory correction to B-tier.
- *Intent taxonomy conflict*: SPEC-FINAL's enum (`define, simulate, instantiate, derive, diagram, evidence, explore, unsure`) and whitepaper Appendix C's sidecar taxonomy (`situate, connect, reflect`, …) both claim the word `intent`. Two vocabularies coexist in the founding set; only Yash can pick the canonical one.
- *Eight gate names*: my enumeration splits one whitepaper sentence into eight ids. Confirm before T1 writes the ledger, since every evidence path keys off them.

**Vocabulary pass (H10)** — words only, `FEATURE-INVENTORY.md` aligned to SPEC-FINAL: thread states → `open/parked/done/archived` + tombstone; anchor states → `exact/moved/unsure/lost`; execution paths kept as four, restoring the zero-model-turn distinction between "re-run the saved solver" and "ask Codex again"; "artifact" → `reply`/`reply_version`; "ledger" → "egress record"; add `cancel_requested` and `outcome_unknown`.

---

## 5. Contradictions to correct in `docs/FABLE-TICKET-STATUS.md` and the receipts

1. **T09/T14/T15 — skills are not loaded.** Status lists `skills/evidence/**` and `skills/explore/**` under Exists; `host-instructions.ts:13` returns early for every intent but `define`, and `tests/jobs-instructions.test.ts:25` encodes define-only loading as intended. Correct to: "skill folders exist and are accurate; none but `define` is loaded, hashed or sent."
2. **T14 receipt overstates retrieval.** It says the transform binds citations to "the exact `FetchedResourceRecord`". `retrieveObserved` is imported only by `tests/retrieval.test.ts` and `recordFetchedResources` has no caller, so `EgressRecord.fetched` is always empty. Correct the receipt and add the same line to T15's.
3. **T15 receipt overstates the renderer.** "The renderer already parks and links shelf items" — `renderer/index.ts:429` emits a plain anchor with no `prepareOpen`, no `returnTo`, no model-suggested label. The receipt's own later correction is the accurate line.
4. **T14/T15 release blocker not recorded anywhere.** Add to both sections: "no evidence or explore reply can be committed today; `capabilities: ['samples']` is hard-coded at `ui/margin.ts:663` and `ui/asking-host.ts:224`."
5. **Consent label vs runtime.** `daemon/consent/service.ts:459` promises web access the runtime cannot give. Record as an open honesty defect against T13/T14 pending H1.
6. **T10 prototype location.** "Exists: historical assisted prototype" — the prototype is at `D:\Projects\Marginalia\src\tools.js`, outside the package; the package contains zero WebMCP lines. Also record that `insert_artifact`/`marginalia.artifact.v1` (ticket 13, `FEATURE-INVENTORY.md:56`) is superseded by `insert_reply`/`marginalia.reply.v1`; the code settles it — `REPLY_SCHEMA = 'marginalia.reply.v1'` at `contracts/reply.ts:4`, and no artifact schema exists.
7. **T11 diagnostics.** "diagnostics allowlist" under Left understates the gap: the module is finished and tested; the HTTP route and the page are what is absent. Also correct the T11 receipt's stale "routes remain T06-owned and pending" (they landed at `daemon/server.ts:190-201`) and soften "exposes real vocabulary list/delete" to note the table is never written.
8. **T16 vocabulary and T16 receipt absence.** As in §3.8; also record that no `T16.md` receipt exists and the work is distributed through T05/T07/T11.
9. **T17.** "Exists: local captured page identity/sections" overstates: identity is `document.title` plus hostname plus a two-valued `pageType` hard-coded to arXiv-or-not (`extension/lib/capture.ts:67`). No date, author or venue is read anywhere. No daemon-side T17 code exists despite the plan assigning daemon work.
10. **T19 receipt.** As in §3.9. Also correct `docs/INSTALL-RECOVERY-EXPERIENCE.md`: the "Show pairing code" change (S2), the unmounted library (§4), the Disconnect defect (S4) and the `/health` Codex probe (S7) are all fixed in current code; and its "Tested on Windows 11, macOS and Linux untested" listing copy conflicts with `docs/T19-CHIEF-DISPOSITION.md`, which records all three as equal targets with a missing Mac as an evidence gap, not a scope reduction.
11. **T12.** As in §3.10; plus the root `GATES.md` is the superseded 2026-09-04 v1 ledger with v1 CHECK paths and pending evidence, and the root `README.md` describes the v1 WebMCP prototype ("No build step or credentials are required"), not the v2 daemon + extension product. Both need additive supersession notes, never a rewrite.
12. **`/health` promise.** `BUILD-PLAN-24H.md`'s T01 row promises "/health reports Codex login, pinned version, sandbox availability"; `daemon/server.ts:97` returns only `{status:'ready', storage:'ready'}`, resolved deliberately in favour of minimal public liveness. Record the resolution so it stops reading as a gap.

---

**Uncertainty carried forward.** I ran no tests, no build and no typecheck; every "tested" claim in the audits rests on reading test source. Line references I re-verified are listed at the top; the rest are the auditing workers' evidence. The isolated-world question (X1), the commit seam in `daemon/jobs/store.ts` (S9), whether a JSON column addition needs a migration (S9), and the Semantic Scholar API terms (H6) are all open and named where they bite.