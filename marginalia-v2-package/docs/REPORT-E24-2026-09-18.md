# E24 report: pre-send local solver capability

Date: 2026-09-18

## Outcome

The Simulate consent review now derives a reader-facing capability sentence from
the host-prepared plan. It displays the exact phrase `can run the model locally`
only when the prepared plan contains `solver` and its preview ID, revision, and
prepared payload digest match the visible consent preview. The outgoing parts are
unchanged and remain visible in the same review. No permission or send path was
added.

## Test totals

- Baseline supplied by the owner: 796 total, 790 passed, 0 failed, 6 skipped.
- Final full suite: 799 total, 793 passed, 0 failed, 6 skipped.
- Focused asking/consent: 77 total, 77 passed, 0 failed, 0 skipped.
- Focused solver gate/service: 67 total, 67 passed, 0 failed, 0 skipped.
- Root TypeScript check: passed.
- Extension prepare and TypeScript check: passed.

The three added tests cover the prepared Simulate handoff, the visible exact
phrase before decision, unchanged outgoing text, no pre-decision dispatch, and
fail-closed behavior for missing or stale capability bindings. Existing solver
tests cover unavailable transport/platform evidence and confirm that no solver
runs.

Raw evidence is retained under `.local/E24/` in `npm-test.log`,
`solver-tests.log`, `typecheck.log`, `prepare.log`, and
`extension-typecheck.log`.

No intermittent concurrent-solver-click or loop-scheduling flake occurred in
this run. No tests were skipped beyond the six existing platform skips.
