# 07-alpha-transform-set

label: wayfinder:grilling
mode: HITL
status: closed
blocked_by: 02-artifact-contract-v1

## Question

Which transforms ship in the alpha and their fidelity contracts: define (read-and-dismiss), instantiate/derive, simulate (Codex execution), evidence card (needs browsing session), connect (library). Pick four to six; everything else is More. Each becomes a skill folder with the Marginalia sidecar.

## Resolution

Resolved. 24h window: define (fast, Luna, read-only), simulate (deep, Astra, workspace-write, network off), evidence and explore (deep, Astra, open session with per-site consent), plus keep and park as reader marks, and the "unsure" reply. Alpha adds instantiate, derive, diagram, connect, reflect chips. Each transform is a skill folder with the Marginalia sidecar.
