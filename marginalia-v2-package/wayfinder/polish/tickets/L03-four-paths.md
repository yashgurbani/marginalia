# Four movement paths on one thread

status: open
type: live, Yash present
blocked by: One real saved-solver recompute

## Goal
The whitepaper names four ways a reply moves, cheapest first: the in-margin kernel, in-envelope samples, saved-solver recompute on the reader's machine, and an explicit new ask. No single thread has crossed all four.

## Acceptance
- One saved thread records, in order: a local slider change, a move answered from samples inside the envelope, an explicit recompute, and a reviewed follow-up ask that changes the model.
- The anchor, the assumptions and the thread identity hold across all four.
- Each step names its path in the rendered outcome. Only the fourth step sends anything.

## Resolution
(pending)
