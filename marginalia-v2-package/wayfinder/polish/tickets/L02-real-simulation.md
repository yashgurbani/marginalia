# One real declarative simulation

status: open
type: live, Yash present
blocked by: One real definition

## Goal
Run one approved "See it" on a passage the fixtures have never seen, such as cooling or growth. No solver authoring in this ticket. This closes the other part of the real-provider evidence gate (Q04, Astra map).

## Acceptance
- The record holds: the reviewed request, a generated model block that passes host validation, a host-backed result sentence or its honest withholding, a slider moved locally with zero requests, a cancel tried on a second run, and a reload with no new call.
- A screenshot of the live reply is captured. Only a reply received live may appear in any media.
- If the model's reply fails validation, record the exact refusal. Then open a build ticket for the skill text.

## Resolution
(pending)
