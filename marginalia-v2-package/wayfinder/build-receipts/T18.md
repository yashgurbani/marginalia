# T18 renderer implementation receipt

Owner: task `01a0addb-ef93-77e0-8a57-45873cead42e`. Shared checkout: `marginalia-v2-package`; no branch switch, reset, commit or push by this task. Chief owns integration.

Current state: **implementation refrozen after the coordinated samples-provenance follow-up on 17 September 2026; validation deferred, not accepted or release-ready.** The user instructed “Leave all testing for later.” All further test authoring, execution, typechecks, builds and browser QA were deferred. Own Pro advisory and two static code-review passes cover the earlier renderer snapshot. Later corrections, including the published T03 samples consumer, were inspected locally but were not tested or sent for another Pro review. The approved genuine host-report and sample-generation-record consumers are implemented; actual consuming UI/helper/store wiring remains a coordinated dependency.

## Ownership

- `renderer/**`: `index.ts`, `state.ts`, `host-authority.ts`, `dom.ts`, `plot.ts`, `diagram.ts`, `reply.css`, `README.md`.
- `kernel/**`: changed `integrate.ts`, `growth.ts`; added `model.ts`, `samples.ts`, `README.md`. `expression.ts` read and included in review packet, not edited by T18.
- `tests/integrate.test.ts` and `tests/renderer-samples.test.ts`: authored by the bounded kernel worker dispatched before the testing deferral. Its completion report arrived as that instruction reached the parent; exact command wall-clock timestamps were not captured in this receipt. No subsequent tests were authored or run by the parent.
- `docs/evidence/T18*` and this receipt.

No T18 edits to contracts, UI, package/build configuration, helper, store or extension. T05 approved the public API and added renderer to its root typecheck scope. T05 retains live margin integration; T04 retains extension wiring.

## Requirement mapping

| Governing requirement | Implementation | Evidence / remaining gap |
| --- | --- | --- |
| Full T18 content vocabulary | Explicit branches for all 17 block type literals; separate ODE/map cases | Static code in `renderer/index.ts`; actual browser acceptance deferred |
| Bounded expressions, RK4/RK45, maps | Existing expression parser retained; model adapter honors `next`, `iterations`, `n`, step, caps, directional events | Worker reported focused checks before subsequent edits; final tests deferred |
| Current parameter interaction, no inference | Native sliders + numeric inputs; synchronous local recomputation; callback boundary only for explicit recompute/follow-up | No inference/network trace has been captured; deferred |
| Correct known growth result | Independent current checks, rationalized lower root, finite-result/range guards; no plotting past a known pole | Numerical regression after these fixes deferred |
| Headline authority | Trusted host injection/current host callback; WebCrypto digest and result binding; immediate stale invalidation and async generation fence | No report means withheld; genuine-report/browser cases deferred; actual host wiring not part of T18 |
| KaTeX and safe text | Packaged KaTeX trust false with limits/MathML; bounded formatting uses text nodes | Static review only; malicious/browser checks deferred |
| Plots and tables | Current numeric trajectories, accessible SVG description, full paged data table, labels and parameter units | Keyboard, focus, clipping, theme/zoom checks deferred |
| Diagram layout | Installed Dagre geometry, packaged SVG labels/groups/edges, readable scrolling and whole-reply dialog | Visual/browser layout not checked |
| Steps/compare/question/turn | Local disclosure; cycle/occurrence bounds; separate selection and explicit sending; immutable version text | Browser QA deferred |
| Citation/shelf/media | Provenance fields and reasons retained; author fetched assertion distinguished; explicit links; honest capability-specific media stubs | Playback intentionally unavailable per T18 stub scope |
| Samples envelope/grid | Complete rectilinear geometry, strict bounds, error evidence and conservative forbidden handling after T03 readiness admission; historical table always retained | Host must supply a genuine matching generation sidecar; no current interpolation without it |
| Four paths | Local math; admitted samples; explicit unambiguously linked solver callback; explicit ask-again | T20 owns real solver, permissions, result validation and commitment |
| Source binding | Hover/focus highlight only; activation navigates; source and return kept in expanded view | Host owns ambiguity and actual document highlighting; browser verification deferred |
| Independent instances and state | UUID namespace plus occurrence IDs; detached snapshots; serialized/coalesced saves; per-instance highlight cleanup and idempotent destroy | Two-instance/reload/late-result browser cases deferred |

## Pro and worker evidence

Own verified **6 Pro** conversation: https://chatgpt.com/c/6aab7cd5-31fc-83ed-9125-c1c2057277e2

The first consultation verified GitHub plugin reads at `f471dd4289806d124c4a9118248e7366ce0bf6e9`, one docs-only commit after supplied `3c724297bbe85ce545943276cebd2704b658963d`. Pro supplied inspected blob IDs. No remote write or implementation branch was requested; all active implementation paths had local ownership. Advisory reconciliation is in `docs/evidence/T18-pro-advisory.md`.

The second submission uploaded `docs/evidence/T18-review-packet.md`, containing the complete unpublished local snapshot and per-file SHA256. It explicitly prohibits tests, builds, browser QA, test authoring or edits. This review is distinct from the published-source advisory.

One bounded native worker was selected as GPT-5.6 Sol High under the then-current ticket instruction, owning only kernel and targeted kernel/sample tests. Its completion report arrived as the user changed subsequent routing to Sol Medium and deferred tests. The parent immediately instructed it to stop further testing; no additional worker was launched. Its launch configuration is exposed by the native spawn; self-description is not used as runtime evidence.

Earlier worker-reported commands:

- `node --test tests/integrate.test.ts tests/expression.test.ts tests/growth.test.ts tests/renderer-samples.test.ts`: 13 passed, 0 failed.
- Scoped strict TypeScript over owned kernel/tests/reply contract: reported pass.
- Repository-wide `npx tsc --noEmit`: reported two implicit-any errors in then-in-flight `renderer/diagram.ts`; parent annotated the point array afterward without rerunning.
- `git diff --check`: reported pass except informational LF/CRLF warnings.

These results predate parent changes to growth formulas, samples geometry/forbidden policy and event crossing. They do **not** establish the final snapshot. The parent has not run a final suite, typecheck, build or browser check.

## Deferred acceptance checklist

When the user resumes testing: typecheck/build and scoped/full tests; actual T00 mount in the margin; slider/numeric changes with request log proving no inference or network; arbitrary starts/equality/out-of-horizon growth results and numeric extremes; host report absence/staleness/mismatch/current binding; sample outside/forbidden/missing context and explicit solver request; malformed payload never executes; keyboard/focus/data table; source hover versus navigation; two instances and comparison duplicate IDs; saved state/reload; expansion/return; themes, 200% zoom and reduced motion; actual T20 recompute integration.

## Cross-owner decisions

- The initial dispatch's wording about browser independent checks was interpreted as local conclusion permission. Chief correctly requested the governing host authority requirement remain intact, then approved genuine host report consumption. `hostReport` and optional `resolveHostReport` now supply that boundary; missing/stale/mismatched reports withhold classification while local numerical interaction remains available. A local-only conclusion policy would require a spec amendment and is not implemented.
- T03 published the additive sample repair: authored axes plus complete non-axis `fixedInputs`, and a separate host-owned generation record binding the reply, model computation, inputs and sample data. T18 consumes this record but never derives or stores one from candidate content. Imported identity records cannot carry execution claims, and matching digests do not certify solver correctness.
- API `mountReply(root, CandidateReply, {sourceText,initialState,capabilities,hostReport,resolveHostReport,sampleGenerationRecords,onStateChange,onSourceHighlight,onSourceNavigate,onRecompute,onFollowup})` returns `getState()`/`destroy()`. Callback host must bind reply-version/thread identity and reject stale solver results using `requestId`/`stateKey`. Host resealing may run installed checks only; no model turn or external retrieval. Parameter and readiness inputs are detached. Digest verification authenticates content binding, not report sender identity.

## Freeze reconciliation

The Pro record `docs/evidence/T18-pro-advisory.md` maps every reported finding to its local correction. The final Pro pass found the reviewed host authority boundary sound by inspection, with no candidate-to-host authority bypass; it identified remaining lifecycle/rendering issues, which were corrected locally afterward. No fourth review or execution check was performed. Uploaded packets retain the exact older reviewed source instead of being silently overwritten.

Final additions include: pre-step known-pole rejection (including event interpolation); reserved-name/finite-growth guards; 512-event recording and paged presentation; SVG/occurrence output bounds; lazy/stable sample history with 25×8 table pages; per-series missing-data handling; separate durable-warning UI; accepted-save draining across unmount; trusted report reuse and resolver fallback; dispatch/result destruction fences; and shared-callback source-highlight restoration across mounts.

Frozen source/test hashes and the observed checkout HEAD are recorded in **`docs/evidence/T18-file-hashes.json`**. This manifest is an identity record, not a build/test result. The HEAD may include other tickets; T18 made no commit. The two pre-deferral worker test files remain owned T18 but unmodified by the parent after the testing instruction.

The samples contract correction is owned by task `01a0ae01-0695-7b61-bd4d-0bacdbce172f` and was integrated by the chief before this follow-up. `ReplyOptions.sampleGenerationRecords` now accepts its exact trusted sidecar type. On every accepted parameter state, the renderer synchronously clears current sample interpolation, then awaits `validateSamplesInterpolationReadiness`. Generation, canonical state and destruction fences reject late results. Only the immutable block and normalized parameters returned by readiness reach `interpolateSamples`; the caller's later mutable state does not. Missing or mismatched evidence remains historical and offers explicit saved-solver recomputation when that path is genuinely connected. No host record is synthesized and no network/model request occurs. T05 received the exact API directly; the chief was asked to relay it to T06 because that owner was not visible in this task's thread list.

Handoff: integrate only the exact owned paths above. Preserve T05/T04 host ownership. Remaining gates are the deferred tests/browser acceptance and real host/store/T20 plumbing; no live provider, extension, sandbox or release gate is established by this ticket.
