# T08 - contextual definition and explicit asking

## Implementation milestone

Base: `b4ad04dd71a6542f3196954a1de129c0ce9ff521`, the documentation-only child of product checkpoint `51e2473f5d26dd3a7e63d6823c9a89528b9d6ad3`.
Branch: `codex/pro-t08-full-recovery`.

The prior sandbox/patch was absent. Four files reconstructed from visible history match the recorded historical Git blob hashes; this is not a claim that the original 14-file artifact was recovered. The current implementation reconciles that history with the pinned host contracts.

`ui/asking/**` now provides exact local page quotations; immutable saved context; explicit host preparation/permission/dispatch; observed availability; read-only reopening; retry/cancellation/unknown fences; actual reply lineage; stable consent and committed-renderer adapters; labelled provisional text; and inline/narrow-sheet child surfaces. The note-bound follow-up uses the supported generic preparation with both parentReplyId and answeredNote, explicitly labelled a new request rather than a resumed provider session.

Local controlled tests currently pass 49/49 on Linux Node 22.16.0 with type stripping. Tests, final evidence and the definition instruction bundle are a separate publication milestone. A strict check against manually transcribed public declaration subsets is not a full repository typecheck. No Node 24, real browser, native runtime or live inference acceptance is claimed.

**T08 remains open for integration and actual reader acceptance.** T05 owns the real reading-position mount, journal acknowledgement, trusted GET/replay transport and design acceptance. T06 owns actual-send, provider readiness, read-only policy, skill loading and network enforcement. T07 persistence is untouched. No integration, backend, provider, shared contract, renderer, library, helper, journal, dependency or global steering file is changed. No additional worker was used.
