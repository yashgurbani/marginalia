# T08 - contextual definition and explicit asking

## Status

**Owned source implemented and locally behavior-tested; not integrated, mounted, or live accepted. T08 remains OPEN for real-reader acceptance.** The source is never rewritten. The work stays as a thread you can come back to.

Base: `b4ad04dd71a6542f3196954a1de129c0ce9ff521` (documentation-only child of product `51e2473f5d26dd3a7e63d6823c9a89528b9d6ad3`). Branch: `codex/pro-t08-full-recovery`. First implementation milestone: `48dc726aa4d8ef688c5784661376a4b02d030fb5`. The final delivery commit carries reviewed corrections, tests, definition instructions and evidence; its exact readback is supplied with the delivery.

## Delivered

`ui/asking/**` implements exact local page quotations; frozen canonical source/capture/anchor/thread/note binding; explicit host preparation and exact consent; observed runtime availability separate from pairing; single approved dispatch; late-callback/security fences; provisional and committed distinctions; cancellation/unknown/new retry; read-only reopening; and exact follow-up lineage. The note-bound follow-up deliberately uses the existing generic parentReplyId+answeredNote request rather than claiming the specialized route preserves a note it omits.

Existing consent and committed renderer are reused through injected adapters. The owned card appends below reader notes at a T05 child slot, preserves its editor/focus, provides inline/modeless narrow-sheet presentations, Source/Change/Keep/Park callbacks, frozen suggestions/More/free text/stated context, labelled provisional text and recorded completion details. It creates no second map, store, grant engine, credential path or generic workflow framework.

`skills/define/**` supplies short contextual-definition instructions, exact quote/interpretation distinctions, at most one clarification, current typed output, and no tool/retrieval/source edits. It is validated as an instruction package, **not claimed loaded or enforced by the baseline provider runtime**. T06 must pin/load it into the actual reviewed envelope and verify the fast-tier read-only policy.

## Evidence and recovery

See `docs/evidence/T08-asking/README.md` for the complete T05 mount contract and actual endpoint sequence, `REQUIREMENTS.md` for every requirement disposition, `SELF-REVIEW.md` for corrections, and `recovery.json` for byte-recovery provenance.

The original 14-file sandbox patch was absent. Four original files were reconstructed and hash-matched during recovery, not all fourteen. After a later sandbox reset, all eight implementation source files were restored against the uploaded milestone blob identities before further editing. No inaccessible original bytes or original TAP output are claimed recovered.

The final controlled run is **53/53 passing** on Linux x64, Node 22.16.0 with explicit type stripping. The final review's pre-fix run is 49 passed / 4 failed and retained as `review-red.tap`; corrected output is `node22-tests.tap`. Compiler syntax checks use available TypeScript 5.8.3, not the pinned 5.9.3 and not a full project semantic typecheck. See `execution.json` and the hash manifest for scope and exact files.

No Node24 target run, full repository build/suite/typecheck, real DOM/browser/zoom/accessibility, native helper transport, provider inference/authentication/account change, Windows or macOS execution was performed. Tests use real owned logic with controlled host/validator/factory/DOM boundaries. They are not source-string assertions or live acceptance.

## Ownership and open gates

Only `ui/asking/**`, `tests/asking*.test.ts`, `skills/define/**`, this evidence directory and this receipt are changed. Integration and other owner branches are not edited. T07 journal/store continuation remains untouched. No additional worker was used; after the route correction, repository operations use GitHub and local code/artifact tools only.

T05 retains actual reading-position mounting, canonical save acknowledgement, native/floating handoff, authenticated GET/replay, renderer-action lifetimes, complete map/thread history, local exposure integration and design/accessibility acceptance. T06/T02/T13 retain actual-send, provider readiness/cancellation/restart, source/consent idempotency and revocation fences, definition skill loading and enforced closed tool network/separate web permission. Windows/Linux/macOS all remain targets. A genuine unseen-term reply, page-defined zero-send path and exact saved-note reader journey are still required. The inventory is neither reduced nor declared complete by this unmounted package.
