# T01 helper receipt

Status: implemented and locally verified; files frozen for chief integration/review. No commit, push, branch switch or edits to other ticket files. Baseline bb5b5b801b285d3f888dd47728be508501261724. Date: 17 September 2026.

## Exact ownership

- daemon/main.ts
- daemon/pairing.ts
- daemon/server.ts
- daemon/diagnostics.ts (new)
- tests/server.test.ts
- tests/pairing.test.ts (new)
- tests/diagnostics.test.ts (new)
- docs/evidence/T01-entrypoint.mjs (new, reproducible runtime smoke)
- docs/evidence/T01-pro-review.md (new, exact-code review packet)
- wayfinder/build-receipts/T01.md (this receipt)

## Behavior

Helper binds only127.0.0.1 and checks exact Host on HTTP/WS. Origins accepted: helper origin and valid chrome/moz extension-origin shapes; possession of one-use pairing code is still required. Tokens are random256-bit values stored by hash and bound to issuing Origin. Renewal via terminal command `pair` invalidates the previous unused code and resets attempts. Expiration includes exact five-minute boundary; five failed attempts exhaust code. Revocation persists and closes matching active sockets.

API paths and existing response fields preserved. `/pair` adds `codex` diagnostics beside `token`. `/health` reports storage readiness separately from Codex installation/login. Same-origin browser GET may omit Origin ONLY with `Sec-Fetch-Site: same-origin`, exact Host and token bound to helper origin. All write requests still require Origin. No development-origin exemption.

`/events` uses existing first message `{token,after}` and response `{type:'events',events}`. Now subscribes to committed durable outbox, drains1000-row batches every100ms, delivers live mutations from HTTP or direct store writers, and resumes from client's last received seq. Auth deadline5s; invalid/future cursors rejected; missing/hostile Origin/Host rejected; revoked auth closed; max64 sockets and slow-reader backlog guard. Events stay available through authenticated `/api/events`.

Startup, health and successful pairing inspect pinned Codex version/login through bounded read-only CLI commands. Raw CLI output, account identifiers and credentials never enter API responses/logs. Installation/login are not execution certification: `sandbox`, `isolation`, `execution` stay `unverified`. `MARGINALIA_DATA_DIR` preserved; `MARGINALIA_PORT` added with integer1..65535 validation. No request/content logging.

## Verification

- `node --test tests/server.test.ts tests/pairing.test.ts tests/diagnostics.test.ts`:10/10 passed. Real TCP HTTP and WebSocket endpoints, no mocked transport. Includes hostile Host/Origin/missing-Origin, strict Bearer syntax, same-origin metadata fallback/token binding, single-use/expiry/attempt exhaustion/renewal, revoke while connected, direct-writer live delivery, reconnect, daemon reopen with durable token/replay/revoke,1002-event replay pagination, no unauthenticated delivery before5s close, diagnostic sanitization/version mismatch/prerelease/missing CLI.
- Targeted strict TypeScript check of all T01 source/test files passed with repository-equivalent compiler options (`node node_modules/typescript/bin/tsc --noEmit --target ES2023 --module NodeNext --moduleResolution NodeNext --strict --allowImportingTsExtensions --erasableSyntaxOnly --skipLibCheck --lib ES2023,DOM` plus owned TS paths).
- `node docs/evidence/T01-entrypoint.mjs`: passed against actual `daemon/main.ts`, disposable SQLite data, terminal renewal, rejected obsolete code, successful HTTP pairing/read/note save, no token/source/note in stdout. Sanitized health at2026-09-17T05:23:01Z: ready storage; Codex installed0.153.4, signed-in; sandbox/isolation/execution unverified. Temporary data removed by harness after helper exit.
- Full shared `npm test`: latest31/32; only concurrent T07 `journal.test.ts` failed durable-save assertion1!==0. Earlier in-progress store `this.replies` failure resolved. T07 notified and requests final integration check after its receipt.
- Full shared `npm run typecheck`: initially passed; latest concurrent T05 ui/margin.ts95,276 TS2488 NodeList missing iterable. T05 notified. Do not misreport as T01 failure or full green build.
- Owned tracked diff whitespace check passed (Git line-ending notices only).

## Pro access and review

Read consult-chatgpt skill. Dedicated fresh Chat tab https://chatgpt.com/ showed workspace out of credits; model menu only Latest/GPT-5.6 Sol/GPT-5.5. No request submitted, model substituted, account switched or payment changed. Chief authorized own-tab read-only access to https://chatgpt.com/c/6aab6e88-3804-83ed-9f27-3f02572b2d98 and Branch in new chat. Visible picker there says `6 Pro`; response actions offered Branch in new chat, but invocation remained disabled Loading for several minutes and never produced a separate URL. No message sent to original conversation. Chief instructed stopping retries and will route consolidated packet after its current Pro work.

Reused chief Pro review (URL above) explicitly says installation/version or tools:[] do not prove isolation; audit effective inherited config/tool catalog and verify actual sandbox before claims. T01 implements that diagnostic distinction. Fresh T01 GitHub branch/commit verification and independent review remain pending via chief. No Pro-written T01 code or commit claimed. Exact current source/tests are in docs/evidence/T01-pro-review.md.

## Remaining gates

Actual extension pairing and actual browser Fetch Metadata behavior are not certified by Node HTTP tests. T05 asked to verify real localhost browser pairing/read and send evidence; T04 extension gate remains separate. No live provider turn, account/read audit, sandbox/network enforcement, OS installer/fresh-machine flow or non-Windows runtime verified here. Existing browser APIs stay compatible; T02/T13 own execution/isolation. Shared full suite/typecheck need rerun after concurrent T05/T07 writers freeze. Chief owns integration and Pro review. No scope removed from feature inventory.

## Browser integration evidence received after freeze

T05 task01a0adc8-a066-7d11-8248-9cb5a63cd737 reports actual in-app browser testing against the built webapp and isolated helper http://127.0.0.1:43125: note written, parked and retained after reload; Settings pairing followed by sync visibly reports saved to the local helper. The exercised path completed POST /pair, queued POST /api/change, and GET /api/threads?removed=true without error. This is T05-reported browser evidence, not a fresh independent T01 run; screenshot/durable artifact pending T05 receipt. It resolves the previously pending real-browser same-origin GET compatibility check. Actual extension pairing and sandbox/provider gates remain unverified. No source files changed after freeze.

## Corrected Pro access verification

Chief supplied the correct current selector route. Fresh own Chat tab https://chatgpt.com/ verified: composer Extra High -> Power control -> End displayed `Pro, 5 of 5`; closing menu displayed exact `6 Pro`. Earlier inference that the model submenu/credit banner meant no Pro access was incorrect. No account/payment changes. No prompt submitted because chief's consolidated T01/T07 review is already running; chief explicitly requested access verification and standby rather than duplicate review. Own tab retained for a scoped follow-up. No separate conversation URL exists until submission.

## Final chief Pro review fixes

Chief's verified GPT-6 Pro review at https://chatgpt.com/c/6aab6e88-3804-83ed-9f27-3f02572b2d98 identified two P2 issues. `POST /api/change` and `POST /api/reattach` now preserve their early authorization check, capture the exact token and origin, then revalidate that pairing after the asynchronous body read and immediately before the synchronous store mutation. A revocation completed while a chunked body is pending therefore prevents the mutation. `/api/reattach` now forwards the optional `capture` metadata as the fourth `ReaderStore.reattach` argument, allowing the store to validate and persist supplied URL/text metadata while preserving the honest legacy path when metadata is absent.

Per chief instruction, no tests, smoke calls, commits or pushes were performed for this final bounded patch. Deferred regressions: an actual chunked-body request revoked between chunks for both mutation routes, plus an HTTP reattach request proving supplied capture metadata reaches the store and a legacy request remains metadata-unknown. The final `daemon/server.ts` SHA-256 is recorded in the chief handoff message after the edit. Source files are frozen again.
