# T05 basic durable margin and design integration

Owner: dedicated Astra design task `01a0adc8-a066-7d11-8248-9cb5a63cd737`. Status: basic implementation frozen for chief integration; **all further testing deferred by user instruction**. Independent Pro read-only review completed; fixes reconciled below but unverified. No commit or push performed by this task. Existing tests were written before the TDD/defer instruction and are not claimed as retrospective TDD.

## Owned files

- `ui/margin.ts`, `ui/margin-model.ts`, `ui/helper.ts`, `ui/persistence.ts`, `ui/margin.css`, `ui/tokens.css`.
- `webapp/main.ts`, `webapp/public/sw.js`.
- `package.json`, `tsconfig.json` for local dev/build/start, extension scripts and UI/webapp/renderer typechecking. No package dependency or lockfile change.
- `tests/margin-model.test.ts`, `tests/margin-design.test.ts`.
- `docs/evidence/T05/**`, this receipt.
- Pro was assigned remote `tests/margin-pro.test.ts` on `codex/pro-t05`; this assignment is now **deferred**. Its GitHub file-write approval was denied after the user said to leave all testing for later. Do not integrate or execute a proposed test file now.

No edits to `ui/journal.ts`, contracts, daemon, kernel, renderer or extension files. Other task changes are preserved. The isolated helper was stopped and its runtime database moved outside Git under `.local/T05-runtime`.

## Delivered basic margin

The missing mountMargin entrypoint is implemented; a production build passed before the testing pause. It uses ReaderJournal with IndexedDB and serialized Web Locks, source-order threads, Keep/Park, note edit and state changes, tombstones with explicit Undo, frozen anchored drafts with explicit Change/whole-page choice, and a recoverable local outbox. Ordinary independent browser tabs have separate drafts; duplicate-tab sessionStorage cloning remains unverified. Notes remain full ink; compact excerpts use size rather than fading. Focus holds the reading position, Follow reading releases it, and only explicit source actions navigate.

Ported the finished Claude design tokens from `D:\UserData\reader\Downloads\marginalia-v1-package\design`, including type/space/colour roles. Preserved restrained surfaces, hairlines, note priority and the coloured section map/rail. Expanded and collapsed states each have one primary map. Omitted the contested summary claim and the mockup's implicit changing draft span. A structural display separator clarifies section quotes without changing stored selectors.

Selecting opens Keep/Ask locally. A conservative literal definition may quote a definition already on the page; suggestions stay stable. Ask previews the exact bounded text and explicitly attached context. There is no inference endpoint or pretend reply. This time/Always are disabled, Never persists and is editable, and the UI states that Codex is not connected. This is preparation for T13, not completed consent enforcement or a connected provider.

Pairing and explicit synchronization use the real helper API. T01's secure same-origin GET handling was exercised in the browser. The production shell service worker caches only local page assets, so the already-installed local page can reload while the helper is unreachable. All private API paths bypass that cache.

## Host seam and deferred T18 integration

`mountMargin(root, { capture, sections, onSource, onHighlight, helperOrigin, storageName, initialOpen, allowHelper, authorizeHelperSend })` returns `{ select(anchor), setReadingPosition(start), destroy() }`. Mount only in a trusted local or extension-origin document. `sourceRoot` is for source text in that same trusted document; external hosts use callbacks. Capture/section offsets must use the same text normalization. T04 was notified; `initialOpen: true` supports an already user-opened host.

T04's Pro review identified clickjacking risk in a page-embedded iframe. `allowHelper: false` now skips pairing-token reads and HelperClient construction and omits Pair/Save-to-helper/Disconnect controls. It directs the reader to the browser-owned margin instead. This alone would not stop an external worker from dispatching the local outbox; T04 reports its reconnect worker is now read-only, requiring an explicit Save in the browser-owned margin. Those new boundary changes are **unverified**. Future inference/consent actions must preserve the same browser-owned boundary.

The optional `authorizeHelperSend(sourceUrl)` host hook runs immediately before each outbox mutation is sent. The UI resolves a keep's captured URL or its existing thread URL and rejects a missing URL. T04 supplies its current exclusion-policy check. Pairing contains no source payload. This closes the direct trusted-margin bypass of the extension's exclusion check; the host policy integration is unverified.

T18 will mount replies below reader notes. Its agreed `mountReply` boundary requires candidate data plus captured source text for defensive validation, current view/parameter state, source highlight/navigation callbacks, explicit recomputation and follow-up callbacks, and destroy/getState. This ticket does **not** claim typed reply coverage, model plots/sliders, source-fidelity lines, host-authorized headlines, assumptions editing, streamed lifecycle states, cancel/retry, or provider outcome handling. Those remain T18/T06/T13/T16 integration work; no inventory is dropped.

## Checks and evidence

Implementation file hashes and modification times: [SHA-256 manifest](../../docs/evidence/T05/file-hashes.json). Source changes remain uncommitted for chief integration.

| Governing requirement | Implementation | Observed evidence before testing was deferred | Remaining gate |
|---|---|---|---|
| Reader-owned durable work without helper | IndexedDB ReaderJournal, Web Locks, separate drafts, offline shell cache | Save/Park/reload, interrupted draft, network-disabled note/reload, multitab draft screenshots | Final save-failure changes, quota exhaustion and later conflict fixes unverified |
| Explicit anchor and source order | Frozen draft anchor, Change, whole-page kind; sorted keyed threads | Note held at scrollY914; exact note/whole-page text after Save | Real-page extension normalization/reattachment, saved-anchor changes |
| Selection sends nothing | Local Keep/Ask/Park, literal definition, stable suggestions | Actual word selection, Keep, Nothing sent | Real provider flow and full suggestion engine |
| Exact consent and chosen context | Explicit packet preview, disabled provider buttons, persistent local denial | Note revision/context payload and denial-after-reload screenshots | T13 host enforcement, recipient/provider configuration and actual send |
| Notes senior and usable at narrow widths | Claude token port, full-ink notes, compact excerpts, horizontal map or collapsed rail | Light/dark360 and440 screenshots; 5 margin tests incl contrast | Real 200% zoom and screen reader session deferred |
| Held reading and deliberate navigation | Focus hold, Follow reading, explicit source callback; highlight callback does not scroll | Focus/caret preserved; Follow reading changes section; hover524→524; source text unchanged | Final focus-return changes and extension host integration deferred |
| Helper pairing and safe reads | Origin-bound token client, exact helper endpoints; explicit sync | Browser pairing status and sanitized POST/GET requests | Extension-origin pairing, recovery after installed-helper restart |
| Rich replies remain in full scope | Agreed T18 mount boundary below notes | Interface coordination only | Full T18 rendering and lifecycle integration, no completeness claim |

- `npm run build`: passes.
- T05-only strict TypeScript command: passes (UI, local entrypoint, Vite config and margin tests).
- `node --test tests/margin*.test.ts`: 5/5 pass. Includes source-order/tombstone behavior, exact payload bounds, literal definition safety, and calculated light/dark text and control contrast.
- Full `npm test`: 93/93 pass at the recorded run.
- Full `npm run typecheck` at that run saw T18's in-progress imports `kernel/model.ts` and `kernel/samples.ts` missing from `renderer/state.ts`. No T05 type error. Recheck at chief integration after T18 files land.
- Actual browser steps and limitations: [browser checks](../../docs/evidence/T05/browser-checks.md), with screenshots and sanitized helper request paths.

These checks are historical evidence, not verification of the final freeze. Later code protects against edits during an in-flight save, preserves a committed mutation when draft clearing fails, offers explicit retry, reloads the journal under the lock before conflict resolution, improves focus return and isolates per-tab drafts. Some received browser/targeted build checks during development, but the final conflict-resolution change, embedded-host helper boundary, and subsequent Pro fixes are **unverified** under the user's stop-testing instruction. No new tests, build verification or browser QA will run until the user resumes them. T07 added durable retention for local stale-revision conflicts; T05 preserves its separate editor draft on rejection and refreshes Settings recovery controls without claiming Save succeeded.

Actual 200% browser zoom, dedicated screen-reader output and forced browser quota failure remain unverified. Unit tests establish token contrast and journal failure behavior; they do not replace these browser checks. No external page extension, live provider, fresh install or release claim.

## Pro evidence

Dedicated conversation: https://chatgpt.com/c/6aab7cb6-cd84-83eb-ac23-6941621ab496 . Before sending, the composer visibly read **6 Pro**, reached through Latest → Power maximum → Escape. The workspace-credit banner did not prevent the consultation. Initial apparent unavailability was corrected; no account or billing change occurred.

One consolidated packet supplied exact local uncommitted UI/helper/storage/service-worker sources and labelled them as local, not published. Pro was asked first to verify the GitHub branch/commit, then review data-loss, privacy, focus and design risks, and write only the exclusive regression-test file on its separate branch if connector writes exist.

Final Pro review: [captured response](../../docs/evidence/T05/pro-review.txt). It reports observed integration HEAD `f471dd4289806d124c4a9118248e7366ce0bf6e9`, one documentation-only commit beyond local `3c724297bbe85ce545943276cebd2704b658963d`, and successful `GitHub.create_branch` for `codex/pro-t05`. `GitHub.create_file` was callable, but its approval was denied after the user deferred testing. No successful test-file write or implementation commit is evidenced, and the branch HEAD was not independently rechecked. This is **not** a read-only connector limitation. No remote test is accepted or integrated.

The initial response continued investigating a test race after the testing pause, so it was stopped. A follow-up explicitly prohibited tests/probes/browser checks/writes and requested a final read-only assessment from existing evidence. The final review establishes no P0 and identifies:

1. **P1, committed-but-uncleared draft:** composer already locks for this state. The additional reload gap is now addressed by saving the exact stable mutation in the draft before journal change, restoring it on reload, and replaying the same identity. Cleanup and recovery keep the composer locked. This change is unverified.
2. **P1, unsaved baseline diverged in another tab:** Retry alone cannot finish. Settings now offers explicit recovery through T07's `reconcilePersistence()` under the shared Web Lock. Durable device state remains authoritative, recovered changes become durable conflicts, and the separate editor draft is preserved. Explicit device-version acceptance calls T07's `acceptCurrentConflict()` so another tab's valid pending upload is preserved; helper-version acceptance uses the existing remote-authoritative resolver. The selected original change remains in resolution history, and any matching editor draft is made editable against the chosen version. All recovery changes are unverified.
3. **P2, focus lost on background revision:** reading controls and footer now stay mounted; thread controls carry stable thread/control keys, and a replaced focused control is restored or falls back to Write here. Focused threads remain expanded. Unverified.
4. **P2, moved quote ordering:** display order, map membership and focused section use only unambiguous current attachment positions. Original anchors are unchanged; unsure/lost work remains explicitly unresolved rather than guessed. Unverified.

The final Pro response qualifies the already-fixed snapshot issues and confirms that disabled provider submission and API-excluding service-worker routing are not established open privacy findings. It does not verify the later fixes.


## Saved-reply integration slice (frozen, source only)

The basic margin was integrated by chief as `aee5750`. This follow-up is a separate bounded slice; it is not full T05 completion. It modifies only `ui/margin.ts`, `ui/persistence.ts`, `ui/helper.ts`, `ui/margin.css`, and T05 evidence/receipt. No tests, build, typecheck, or product browser QA have run for this slice.

- Real `mountReply` is inserted beneath reader notes for immutable stored ReplyVersions. The captured original SourceVersion is used for renderer validation. Current-page navigation separately requires a unique exact/context attachment; unresolved source passages do not navigate.
- Authenticated helper reads populate a per-origin/thread/reply IndexedDB cache. Current view state is stored separately from authored reply content. Independent saved versions and their answered note version are visible; no fixture or generated reply is presented as live work.
- View snapshots use a shared per-reply queue plus Web Locks, local revision checks, and durable stable mutation IDs for helper revision-CAS writes. Conflicting local inputs are retained in recovery exports. Explicit helper-view restoration retains the former local state in that export.
- Reply controls stay mounted when notes or thread state change. A final buffered snapshot is queued before reply destruction; late callbacks from that renderer are ignored. Margin remounts drain accepted local work; root keydown listeners abort, and destroy removes only its own DOM. Source highlights do not scroll, and focused controls hold the reading position.
- Genuine cached commit/check reports are passed to the renderer. Optional authenticated `/api/reply-check` runs installed `runHostChecks` only with exact current parameters. The renderer owns digest/generation validation. No candidate field is treated as host authority.
- T18's `sampleGenerationRecords` is a separate trusted sidecar keyed by sample block ID. T06 currently delivers no generation records; none are manufactured. Authored sample grids remain historical unless genuine records become available.
- `allowHelper:false` never constructs a client, reads pairing credentials, or exposes helper save/restore/check callbacks. Cached replies and local controls remain available there. Trusted helper requests recheck the host's exclusion policy.

T06 owns the actual authenticated server routes: GET `/api/replies?threadId=`, GET `/api/reply-view?threadId=&replyVersionId=`, POST `/api/reply-view`, POST `/api/reply-check`. Client method shapes were coordinated with that owner. Actual runtime endpoint integration remains unverified.

Pro source review was submitted once in a fresh conversation after visibly selecting **6 Pro** (Power: Pro, 5 of 5). Conversation: https://chatgpt.com/c/6aab8b45-24b4-83eb-bc5a-c065de95d880 . Exact reviewed snapshot: `docs/evidence/T05/reply-pro-packet.txt`. The request explicitly forbids tests, execution, probes, browser QA, connector access and writes. Review reconciliation is recorded in `docs/evidence/T05/reply-review-reconciliation.md`; subsequent source fixes are not retroactively covered by the submitted snapshot.

Remaining product integration: real Ask + T13 consent/grants + T06 job progress/cancel/retry/follow-up; explicit saved-solver execution; library entrypoint; genuine sample-generation delivery; full new Claude frame 6–14 design; deferred runtime and accessibility verification. This slice deliberately leaves unavailable callbacks unset and labels that limitation in the product.


The saved-reply slice is now frozen. Pro's three P1 and three P2 findings are reconciled in source in [reply review reconciliation](../../docs/evidence/T05/reply-review-reconciliation.md). Exact slice identities, including explicitly labelled external dependency snapshots, are in [reply SHA-256 manifest](../../docs/evidence/T05/reply-file-hashes.json). T06 confirmed that `/api/replies` now includes persisted removed versions; the client uses their real monotonic tombstones, never inferred deletion. No runtime verification or second Pro approval is claimed. No commit/push was performed by T05.

## Final source freeze before consolidated review

Chief's subsequent source review found one P2 for recovery-history write growth and no P0/P1 in that snapshot. The bounded follow-up changes only `ui/persistence.ts`: independent content-addressed history keys preserve unique recovered states and rejected mutation identities, deduplicate equal states, and atomically migrate legacy inline arrays while keeping the frequently written current record small. List/export hydrates retained history. No count cap drops unique work. Full export may still grow with actual history. This source change is untested.

The design assessment is `docs/DESIGN-PASS-2-RECONCILIATION.md`. It records the user's reading-position editor and one information-preserving map decisions, covers frames 5+ and 6–14, separates refinements from remaining product/contract choices, and preserves all unfinished scope. No pass-two product implementation or original export changed. Source is now frozen for chief consolidation and the newly authorized detailed 6 Pro review. No tests, builds, typechecks or product probes were run.