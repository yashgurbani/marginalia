# T14 Evidence

Owner: Opus worker `opus-t14-t15` (Claude Opus 4.8, Medium). Codex is the controlling agent.
Base: `51e2473f5d26dd3a7e63d6823c9a89528b9d6ad3`. Branch: `codex/opus-t14-t15-evidence-explore`.
Head: filled by the final docs commit on this branch.

Status: historical Opus milestone below; Sol evidence correction recorded at the end. Integration
wiring (host builds observations from the egress record) and live acceptance are open and belong to
T06 Pro. No stage gate is claimed passed.

## Ownership and delivered source

- `daemon/transforms/evidence/reconcile.ts` — host reconciliation transform (new)
- `daemon/transforms/evidence/index.ts` — public barrel (new)
- `skills/evidence/SKILL.md`, `skills/evidence/IO.md` — skill instructions and IO contract (new)
- `tests/evidence-transform.test.ts` — regression tests (new)
- `docs/evidence/T14-T15-opus/**`, this receipt

No shared contract, broker, job, provider, consent, server, renderer, ui, webapp or manifest file
was edited. The transform reuses the existing `CitationsBlock`, `FetchedResourceRecord`,
`EgressRecord` and `ConsentScope` contracts; it introduces no parallel schema or broker.

## Requirements disposition

| Requirement | Disposition |
|---|---|
| Separate the reader's claim from dated support | Implemented. Each entry keeps `claim` and `support` distinct; the transform never merges them. |
| Abstain without attributable support | Implemented. `verdict: insufficient` when no entry resolves to an observed fetch; the evidence headline is withheld. |
| A URL or model citation is not a fetched source | Implemented. A `fetched:true` entry with no matching observed record is neutralized to `unsupported-fetch-claim` and flagged. |
| Each substantive support resolves to a real host retrieval record | Implemented. `observed-fetch` binds the exact `FetchedResourceRecord` (finalUrl, sha256, fetchedAt, outcome). |
| Dates identify what is known and unknown | Implemented. `dates.claimedSourceDate` is model-declared and marked unverified; `dates.retrievalDate` is the host-observed fetch date, or null. |
| Closed sessions perform no retrieval | Implemented. A closed session with observed records is `refused`; a fetch claim in a closed session is `unsupported`. |
| Do not accept incomplete logs as complete | Implemented. `retrievalComplete:false` downgrades a real fetch to `unresolved` and withholds the headline. |
| Preserve source/consulted identity and completeness truth | Implemented. `boundSourceVersion`, `retrievalComplete`, and per-entry records are carried through. Stale source binding is detected. |
| Do not weaken SSRF/redirect limits or fabricate confinement | Held. The transform performs no IO and makes no confinement claim; the broker's limits are untouched. |
| Explain missing/insufficient/partial evidence calmly | Implemented. Per-entry `notes` and `reason` state the outcome plainly. |

## Commands and results

```
node --test tests/evidence-transform.test.ts   # 13 tests, pass 13, fail 0
node --test tests/*.test.ts                     # 316 tests, pass 315, skip 1, fail 0
node node_modules/typescript/bin/tsc --noEmit   # exit 0
```

`node_modules` was absent at session start and later present as a shared junction; the typecheck
and full suite were run against that shared install. No dependency was installed and no manifest or
lockfile was changed.

## Public interface (for T06 Pro integration)

```ts
reconcileEvidence(reply: CandidateReply, observations: EvidenceObservations): EvidenceAssessment
```

`observations` is built by the host from records it already owns:
- `sessionScope` from the consent scope of the job.
- `retrievalComplete` from `EgressRecord.retrievalComplete`.
- `observed` from `EgressRecord.fetched` (`FetchedResourceRecord[]`).
- `boundSourceVersion` from `FrozenJobContext.sourceVersionId` + `sourceHash`.
- `currentSourceVersion` when the reply is re-checked against a newer capture.

Required wiring (T06 owns the change): after a validated evidence reply, assemble `observations`
from the attempt's egress record and call `reconcileEvidence`; store the `EvidenceAssessment`
alongside the host check report; expose `verdict`, `headline` and per-entry `attribution` to the
renderer as retrieval facts separate from the author-only citation string in
`renderer/index.ts` (`case 'citations'`). The correction below withholds claim authority.

## Limitations

- Page-text containment ("the fetched page contains the attributed text") is not verified here;
  `textVerified` is always `false`. That needs the fetched body and is a separate A-priority step.
- Fixtures are labelled fixtures. They do not constitute the real Concurrent/Lean/Clay acceptance
  journey or any live retrieval.
- No live provider inference, no live broker fetch, and no browser QA were performed.

## Sol evidence correction — 17 September 2026

Base: `86d658191cb6e8a6c1bd8bac45ec453c80a964e5` on
`codex/wave-sol-evidence`. This section supersedes historical `supported`,
`observed support` and headline claims above; the original milestone remains traceable.

- A complete reply with a current attributed retrieval now yields `unverified`, never
  `supported`. `headline` stays null because retrieval cannot check claim meaning or page text.
  `observedFetchCount`, the fetched record and retrieval date remain available as facts.
- `citationUrlMatch` distinguishes a match to requested URL, final URL or both. On redirect,
  the record keeps the actual final resource and a requested-only citation gets an explicit note.
  A legitimate redirect remains an observed retrieval; it grants no claim authority.
- `replyStatus` preserves validated partial versus complete status. Partial replies remain
  `insufficient`, even when they contain an observed fetch.
- T06 caller must pass a schema-validated reply and observations from the *same producing
  attempt's* egress record and frozen source. This pure transform does not enforce attempt
  identity or validation; wiring must establish that boundary before presenting the result.
- Focused `node --test tests/evidence-transform.test.ts`: 16 pass, 0 fail. Project
  `node node_modules/typescript/bin/tsc --noEmit`: exit 0. No full suite or live acceptance
  claimed for this correction.

Remaining gates: T06 same-attempt composition; T05 display of retrieval facts without
claim-level authority; actual page-text/semantic checking if a support verdict is to be
offered; stage 5a live permission, broker accounting, and reader journey on real pages.
