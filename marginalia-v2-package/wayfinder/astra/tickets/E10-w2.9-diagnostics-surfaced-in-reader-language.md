# E10 W2.9: diagnostics surfaced in reader language

label: wayfinder:task
mode: AFK
status: open
blocked_by: E01, E05
route: Sol medium

## Task
`docs/FEATURE-STRATEGY-2026-09-18.md` Stage 2 W2.9 and T19's "diagnostics page". The daemon already computes a `DiagnosticsResult` (see `daemon/` diagnostics helper with a 30-second cache). Surface it: a read-only "How things are" section in the margin settings and in the extension options page: helper address and reachability, pairing state, data directory, Codex sign-in state (signed in / signed out / not found, never a token), versions, last migration backup if any. Every line is a fact the reader can act on; each failure state names the one next step.

## Acceptance
- Renders from the existing GET route without new writes; opening it sends nothing to a model.
- Tests for signed-out Codex and unreachable helper states.
