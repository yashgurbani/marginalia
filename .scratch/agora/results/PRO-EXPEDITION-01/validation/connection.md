# Connection gate

Frozen relation vocabulary:

- `prerequisite`
- `analogy`
- `contradiction`
- `enables`
- `bridge`
- `example`

Observed cases:

1. Unseen note path `physics/not-returned.md` returned `ok:false`, `error:"precondition_failed"`, with a corrective `next_step`.
2. Unsupported relation `similar` returned the normal contract error shape.
3. Path returned by `search_notes`, relation `bridge`, and visible reason produced an `author:"agent"` artifact.
4. Renderer produced one connection block showing relation, target path, reason, and existing Remove control.
5. Graph data derived one edge from the same artifact.

Commands: tool and integration diagnostics.  
Result: `ALL PASS`.
