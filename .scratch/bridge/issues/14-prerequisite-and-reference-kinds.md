# 14 — `prerequisite` inset + role-typed `reference`
**What to build:** `annotate(kind: prerequisite)` renders an attributed inset chapter before a section (≤600 words, sources or "generated" badge); `reference` renders with role chips (read-first / see-also / origin / counterpoint / data).
**Blocked by:** 05
**Status:** ready-for-agent
- [ ] Inset is visually distinct from source (margin color, "agent" badge), collapsible, removable
- [ ] Density caps inset depth: a second inset under an inset requires the section's concepts at `unknown`
