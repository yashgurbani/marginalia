# W2 — [wayfinder:research + grilling] Connectors with consent (Drive, Zotero, newsletters)

## Question
Without a local daemon, which consent and egress patterns from FeynmanILE `providers.md` and Noether IRE `providers.rs` apply to browser-side OAuth? What must never reach the agent (raw doc bytes vs. snapshot text)? Research: Drive API from a static origin; Zotero OAuth reuse from Noether IRE.

**Blocked by:** W1
